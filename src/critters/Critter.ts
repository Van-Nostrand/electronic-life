// import { TCoordinates } from '@/types'
// import { ICritterProps, ICritter } from '@/critters/types'

// export default function Critter (props: ICritterProps): ICritter {

//   return ({
//     ...props,
//     view: null,
//     setPosition (newPos): void {
//       this.x = newPos.x
//       this.y = newPos.y
//     },

//     setFacing (newFacing: TCoordinates): void {
//       this.facing = newFacing
//     },

//     takeTurn (world?: string[][]): ICritter | Error { world; return this }
//   })
// }
